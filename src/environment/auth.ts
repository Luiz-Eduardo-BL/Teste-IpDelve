// import { get_auth_token } from './api';

// async function login(username: string, password: string): Promise<string | null> {
//   const token = await get_auth_token(username, password);
//   return token;
// }

// export default login;